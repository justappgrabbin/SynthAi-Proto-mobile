# User's Actual Project Inventory

## What Actually Exists (From GitHub)

### 1. **Simlish** (C# + React)
- FSO-based simulation engine
- Multiple consciousness engines (C#):
  - `AutonomousAgentEngine.cs`
  - `CodonSpectrumEngine.cs`
  - `ConsciousnessEquationEngine.cs`
  - `ResonanceSentenceEngine.cs` (YOU-N-I-VERSE routing)
  - `GameImplementationEngine.cs`
  - `SimulationEngine.cs`
  - `HarmonicFrequencyEngine.cs`
  - `NeuralEngine.cs`
  - `DreamSequenceEngine.cs`
- React components for POV experience
- Multiple guides and documentation
- **Status**: Code exists but unclear which versions are actually working

### 2. **cuddly-doodle** (Python + React)
- Flask backend (`app.py`, `api_routes.py`)
- Consciousness field engine (`consciousness_field_engine.py`)
- Avatar engine (`avatar_engine.py`)
- Book processor (`book_processor.py`)
- Collapse engine (`collapse_engine.py`)
- Field scanner (`field_scanner.py`)
- Ephemeris calculations (`ephemeris.py`)
- Drizzle ORM config (TypeScript)
- React components
- **Status**: Multiple engines built but unclear if they're integrated

### 3. **Resonance-Network** (React Native/Expo)
- Mobile app (iOS/Android)
- ConsciousnessEngine.js (placeholder for real logic)
- Self-bootstrapping system (`SystemBootstrap.js`)
- Revenue model built in (subscriptions, consultations)
- Profile matching system
- **Status**: Framework exists, needs consciousness engine plugged in

## What's Actually Working vs. Broken

### ✅ Likely Working:
- Basic code structure and scaffolding
- Database schemas
- UI frameworks
- File organization

### ❌ Likely Broken/Incomplete:
- Integration between components
- Consciousness engine routing
- Material ingestion pipeline
- End-to-end workflows
- Deployment/hosting
- Multiple websites with no content

## The Real Problem

User has **three separate projects** with pieces of the system scattered across them:
1. **Simlish** - C# consciousness engines (FSO simulation)
2. **cuddly-doodle** - Python consciousness engines (field analysis)
3. **Resonance-Network** - Mobile app (needs consciousness engine)

**Plus**: Multiple websites that don't work, versions that may or may not be current, and code that may have been modified by mCP but user doesn't know if it's the right version.

## What User Actually Needs

1. **A router** that can ingest code from all three projects
2. **An orchestrator** that understands which pieces go where
3. **A learning system** that figures out how to integrate them
4. **Deployment** so it's all live and safe
5. **Clarity** on which versions are actually the "real" ones

## Recommendation

Before building the smart ingestion engine, need to:
1. **Audit which code is actually current** (Simlish vs cuddly-doodle vs Resonance-Network)
2. **Identify the working consciousness engines**
3. **Map the YOU-N-I-VERSE routing logic** from the code
4. **Build the orchestrator** to integrate them
5. **Deploy** as a unified system

This is more complex than initially thought because the code is fragmented across three repos with unclear versions.
